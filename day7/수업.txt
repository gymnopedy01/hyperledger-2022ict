couchdb 조회
http://localhost:5984/_utils/#/_all_dbs

원장에 데이터를 쓰는거
원장에 데이터를 읽어오고 다시 쓰는거


networkDown.sh 종료
startFabirc.sh


$ cd ~/fabric-samples/fabcar
$ ./startFabric.sh
enroll


fabric-samples 와 강사가 제공한 git 두예제의 차이점
 ca를 이용해서 인증서를 이용하여 생성하느냐, crypto-config.yaml 을 이용하느냐에 두가지 차이점이 있다.
 ca를 사용할때는 id 와 password 가 들어감.

$ peer chaincode invoke
$ peer chaincode query -C ${채널명} -n ${} -c ${트랜잭션명} '{}'

$ cd javascript
$ npm install

$ node enrollAdmin.js
$ cat wallet/admin.js
$ node registerUser.js
$ cat wallet/appUser.js

$ node query.js
$ node invoke.js

$ docker logs pee0.org1.example.com

GetFunctionAndParameters()


go 제공되는 라이브러리 두가지
shim : 체인코드에 기록
peer : 결과에 대해서 어떻게 리턴해줄것인가를 정의해준 라이브러리



9시 10시 재수강!!